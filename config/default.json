{
    "routes": [
        {
            "proxies": [
                {
                    "binding": "date",
                    "url": "http://date.jsontest.com",
                    "folder": "date"
                }
            ],
            "paths": [ "/:path" ],
            "method": "get",
            "folder": "jsontest"
        }
    ]
}